{
  "name": "knict-fetch",
  "version": "0.1.16",
  "description": "",
  "main": "bin/index.js",
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc && rm -rf ./bin/* && mkdir -p ./bin && cp -r ./dist/* ./bin",
    "test": "yarn build && node ./dist/test/test.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.4",
    "knict": "^0.2.6"
  },
  "devDependencies": {
    "typescript": "^4.4.3"
  }
}
