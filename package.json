{
  "name": "knict-fetch",
  "version": "0.3.0",
  "description": "",
  "main": "bin/index.js",
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc && rm -rf ./bin/* && mkdir -p ./bin && cp -r ./dist/* ./bin",
    "test": "yarn build && node ./dist/test/test.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.4",
    "form-data": "^4.0.0",
    "knict": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "typescript": "^4.4.3"
  }
}
