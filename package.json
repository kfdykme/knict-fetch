{
  "name": "knict-fetch",
  "version": "0.1.6",
  "description": "",
  "main": "bin/index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc && rm -rf ./bin/* && mkdir -p ./bin && cp -r ./dist/* ./bin",
    "test": "yarn build && node ./dist/test/test.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "knict": "^0.1.11"
  },
  "devDependencies": {
    "axios": "^0.21.4",
    "typescript": "^4.4.3"
  }
}
